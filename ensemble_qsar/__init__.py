"""MDS-ensemble QSAR: conformational-ensemble descriptors for flexible molecules.

Stage 1 (this package's `data` module) turns a public property benchmark into a
curated, descriptor-annotated dataset and selects a flexibility-stratified
representative set for the downstream molecular-dynamics + conformer-extraction
stage.
"""

__version__ = "0.1.0"
